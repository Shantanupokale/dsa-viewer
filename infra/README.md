# infra/ — deployment config (later)

Backend needs a real VM (Docker-in-Docker) → Fly.io or small VPS (`fly.toml` here).
Frontend → Vercel. Not needed until there's something to deploy; scaffolded now for shape.
